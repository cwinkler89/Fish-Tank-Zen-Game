#ifndef SETTINGS_H
#define SETTINGS_H


class Settings
{
public:

    static bool showPlayerEnergy;
    static bool showPlayerSightRadius;
    static bool showPlayerSize;

    static bool showSmallerEnergy;
    static bool showSmallerSightRadius;
    static bool showSmallerSize;

    static bool showBiggerEnergy;
    static bool showBiggerSightRadius;
    static bool showBiggerSize;

    static bool showHelpScreen;
    static bool showCredits;

    static int startSize;
    static int maxFishSize;
    static int minFishSize;

    static int maxFrameRate;


};


#endif